name: tags

on:
  push:
    # branches:
    #   - main
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+(-[A-Za-z0-9])*"

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: |
          echo "hello"
